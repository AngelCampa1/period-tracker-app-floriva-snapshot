# Floriva Pristine Sweep - 2026-06-11

This folder records the current long-running pristine sweep evidence. Prior sweeps were treated as reference only; the commands below were rerun locally against the `codex/pristine-sweep` worktree.

## Fixes Applied In This Slice

- Added root app `StatusBar style="dark"` so Android status icons are readable against Floriva's light background.
- Updated the store screenshot harness to resolve configured output directories to absolute paths before passing them to native screenshot commands.
- Kept the import screenshot excluded from the default `qa-rich-history` capture and tied it to the `import-ready` preset.
- Stabilized Android condition-mode E2E scrolling on the daily logging screen with a bounded retry helper that scrolls until each expected condition row is actually visible.
- Stabilized fresh onboarding smoke and persistence E2E decision steps by retrying the full option-plus-Continue interaction until the next screen is reached, avoiding disabled Continue loops when a selection tap misses.
- Gated the backup export E2E flow to no-preset runs so `backup-ready` restore-preview runs no longer execute an unrelated export assertion.
- Extended the `backup-ready` native restore E2E flow from preview-only to acknowledge, confirm, status, and SQLite row-level restore verification on both platforms.
- Preserved the existing CycleRibbon readability fix: phase labels render as a wrapped legend instead of being constrained to narrow segment widths, with a constrained-width render assertion.
- Removed reproductive-health wording from OS notification titles/bodies for cycle and birth-control reminders; the in-app reminder center can stay specific, but lock-screen notifications now remain generic.
- Added a dev-only, redacted scheduled-notification diagnostics hook gated by `EXPO_PUBLIC_E2E_SCHEDULED_NOTIFICATIONS=1` so E2E can assert real OS schedules without exposing payload data in production.
- Hardened the store screenshot harness for iOS dev-client runs by blacklisting Metro/dev-server URLs after synchronization is disabled, and made import-only capture fail fast unless the `import-ready` preset is active.
- Tightened the Birth control settings reminder controls so the primary on/off action stays prominent while Earlier/Later timing adjustments share one compact row with 48px minimum tap height.
- Refreshed notification-plan tests so OS notification copy is asserted as generic and private while date arithmetic remains covered through trigger dates.
- Clipped the shared back-action pill to its rounded shape on Android and cleaned the Android marketing screenshot status bar by hiding demo network indicators that produced warning glyphs on the light background.
- Added `DETOX_IOS_DEVICE_ID` support to `detox.config.js` so blocked iOS reruns can target a freshly created simulator UUID instead of repeatedly selecting stale named devices.
- Hardened the delete-all-data confirmation: final confirmation now uses the destructive button treatment, disables confirm/cancel while the wipe is running, and keeps the user on the screen with a localized error if deletion fails.
- Added a no-preset native delete-all-data E2E spec that completes real onboarding, writes a real note, deletes local data from Settings, and cold-relaunches without clearing the container to prove onboarding returns instead of persisted tracker state.
- Extended the delete-all-data E2E spec to Android by removing the Android skip, explicitly clearing Android app data before the fresh launch, dismissing Expo's dev-client menu through visible UI nodes, filtering offscreen `uiautomator` nodes against the current Android viewport, and using a visible logging chip plus direct SQLite row-count checks for the Android persisted-data probe.
- Hardened the iOS delete-all-data E2E assertion so cold relaunch accepts either onboarding entry route while still rejecting the Today app shell and the deleted probe note.
- Increased shared floating-tab content clearance so the final content card clears the dock on dense tab screens such as Today, with a regression assertion tied to dock height plus one minimum tap target.
- Strengthened active onboarding Continue buttons with a raised primary treatment while keeping disabled actions flat and dim, addressing active/disabled ambiguity in the StoreKit onboarding screenshots.
- Changed the first-run welcome headline from "A private almanac for your cycle." to the plainer "A private tracker for your cycle."
- Simplified the Calendar key: the fertile-window help control now sits in a separate header row, the legend wraps as compact chips, and the new visible label is localized across supported languages.
- Fixed recovered lint regressions in the uncommitted E2E slice by declaring Detox's `beforeEach` global in the Android picker spec and importing Node's `Buffer` in the native database-copy helpers.
- Expanded Android DocumentsUI import-picker coverage from Clue/cancel/malformed JSON to include real Flo export selection and unsupported media-file rejection.
- Strengthened destructive `ActionButton` styling from a pale outline treatment to a solid danger fill with light text, so final delete-all-data confirmation reads as an irreversible action without relying on copy alone.

## Automated Verification

| Command | Result | Notes |
| --- | --- | --- |
| `corepack pnpm test -- --runInBand tests/components/editorial/CycleRibbon.test.tsx` | Pass | 10 tests. |
| `corepack pnpm test -- --runInBand tests/app/root-layout.test.tsx` | Pass | 8 tests, includes root status-bar coverage. |
| `corepack pnpm test -- --runInBand tests/app/root-layout.test.tsx tests/components/editorial/CycleRibbon.test.tsx` | Pass | 17 tests. |
| `corepack pnpm test -- --runInBand tests/lib/notifications/reminderScheduler.test.ts tests/lib/notifications/scheduledNotificationDiagnostics.test.ts tests/features/settings/SettingsScreen.test.tsx` | Pass | 70 tests, including redacted notification copy and dev-only diagnostics gating. |
| `NODE_OPTIONS=--max-old-space-size=8192 corepack pnpm test -- --runInBand tests/features/settings/SettingsScreen.test.tsx tests/app/settings-birth-control-route.test.tsx` | Pass | 61 tests, including the Birth control reminder adjustment row layout and 48px tap-height assertion. |
| `corepack pnpm test -- --runInBand tests/lib/notifications/buildReminderPlans.test.ts tests/lib/notifications/buildReminderPlans.probe.adversarial.test.ts tests/lib/notifications/reminderScheduler.test.ts tests/lib/notifications/scheduledNotificationDiagnostics.test.ts` | Pass | 45 tests, including generic OS notification copy and trigger-date arithmetic. |
| `corepack pnpm test -- --runInBand tests/components/Screen.test.tsx tests/features/settings/SettingsScreen.test.tsx tests/app/settings-birth-control-route.test.tsx` | Pass | 82 tests, including back-action pill clipping and Birth control reminder layout. |
| `corepack pnpm test -- --runInBand tests/sanity/detox-config.test.ts` | Pass | 3 tests, including UUID-targeted iOS Detox device selection for diagnostic reruns. |
| `corepack pnpm test -- --runInBand tests/components/Screen.test.tsx tests/features/settings/SettingsScreen.test.tsx tests/features/onboarding/shared.test.tsx tests/features/onboarding/WelcomeScreen.test.tsx tests/app/onboarding-smoke.test.tsx tests/app/smoke.test.tsx tests/sanity/detox-config.test.ts` | Pass | 111 tests, including delete-data failure/pending states, tab clearance, onboarding CTA treatment, welcome copy, and Detox UUID selection. |
| `node --check e2e/delete-all-data.e2e.js` | Pass | Validates the new native delete-all-data E2E spec parses. |
| `node --check e2e/store-screenshots.e2e.js` | Pass | Validates the screenshot harness edit parses. |
| `corepack pnpm test -- --runInBand tests/features/calendar/CalendarScreen.test.tsx tests/localization/translations.test.ts` | Pass | 33 tests, including the Calendar key/help-row layout contract and localization completeness. |
| `corepack pnpm test -- --runInBand tests/sanity/delete-all-data-e2e.test.ts` | Pass | Source-level regression guard for Android delete-all-data coverage, deterministic Android app-data clearing, dev-menu dismissal, viewport-filtered Android UI parsing, direct Android SQLite probe checks, and the shared deleted-state onboarding assertion. Runtime proof is in the native Detox rows below. |
| `corepack pnpm test -- --runInBand tests/components/editorial/CycleRibbon.test.tsx tests/components/Screen.test.tsx tests/features/settings/SettingsScreen.test.tsx tests/features/calendar/CalendarScreen.test.tsx tests/features/onboarding/shared.test.tsx tests/features/onboarding/WelcomeScreen.test.tsx tests/app/root-layout.test.tsx tests/sanity/detox-config.test.ts tests/sanity/delete-all-data-e2e.test.ts tests/sanity/android-import-picker-e2e.test.ts tests/sanity/backup-restore-e2e.test.ts tests/lib/notifications/buildReminderPlans.test.ts tests/lib/notifications/buildReminderPlans.probe.adversarial.test.ts tests/lib/notifications/reminderScheduler.test.ts tests/lib/notifications/scheduledNotificationDiagnostics.test.ts` | Pass | 15 suites, 187 tests. Rerun after recovering the prior worktree and fixing the E2E lint regressions. |
| `node --check e2e/android-import-picker.e2e.js` | Pass | Validates the expanded Android DocumentsUI picker spec parses. |
| `corepack pnpm test -- --runInBand tests/sanity/android-import-picker-e2e.test.ts` | Pass | Source-level guard covers Clue, Flo, cancel, malformed JSON, and unsupported media picker paths. Native runtime proof passed in `detox-android-import-picker-expanded-main-rerun7`. |
| `corepack pnpm test -- --runInBand tests/components/ActionButton.test.tsx tests/sanity/android-import-picker-e2e.test.ts tests/features/settings/SettingsScreen.test.tsx` | Pass | 3 suites, 72 tests. Covers stronger destructive button tokens, Android picker source guard, and delete-data/settings behavior. |
| `corepack pnpm test -- --runInBand tests/localization/translations.test.ts tests/features/import/ImportScreen.test.tsx tests/features/import/ImportFlowProvider.test.tsx tests/sanity/android-import-picker-e2e.test.ts` | Pass | 4 suites, 56 tests. Rerun after Android picker native fixes, media MIME reachability, and JSON error-copy polish. |
| `corepack pnpm typecheck` | Pass | No TypeScript errors. |
| `corepack pnpm lint` | Pass with warnings | 13 existing warnings in unrelated adversarial tests. |
| `corepack pnpm test:coverage:check` | Pass | 206 suites, 3466 tests, coverage checker passed after the Android picker/native import fixes. React `act(...)` warnings appeared in insights detail tests; Expo notifications emitted the known Expo Go warning during settings imports. |

## Native And E2E Verification

| Flow | Result | Evidence |
| --- | --- | --- |
| iOS native build via XcodeBuildMCP / Xcode | Pass with MCP timeout caveat | `ios/build/Build/Products/Debug-iphonesimulator/Floriva.app` was produced and installed manually after MCP timed out waiting. |
| iOS manual `qa-rich-history` launch | Pass | `ios/live-sweep/manual-qa-rich-history-after-reset.jpg`. |
| iOS `FlorivaStoreKit` MCP build/launch rerun | Partial | XcodeBuildMCP active profile `floriva-storekit` used workspace `ios/Floriva.xcworkspace`, scheme `FlorivaStoreKit`, simulator `iPhone 17` (`2B9F547F-E6A8-409B-85EE-968CBA23DE20`), and bundle `app.floriva`: build/install/launch succeeded. Screenshots: `ios/storekit-mcp-rerun/launch.jpg` and `subscribe-top.jpg`. The subscribe screen rendered, but the plan CTA stayed disabled because products were not loaded when launched headlessly; the scheme references `../Floriva.storekit`, product IDs match `.env.local`, `xcrun storekit` is unavailable, `simctl launch` has no StoreKit configuration option, and `xcodebuild -help` exposes no StoreKit testing launch flag. Transaction completion still needs the Xcode GUI Run action with StoreKit testing attached or TestFlight/real-device sandbox. |
| iOS store-surface capture, `qa-rich-history` | Pass | `env DETOX_IOS_DEVICE_ID=2B9F547F-E6A8-409B-85EE-968CBA23DE20 EXPO_DEV_SERVER_PORT=8081 EXPO_PUBLIC_DEV_LAUNCH_PRESET=qa-rich-history FLORIVA_CAPTURE=1 FLORIVA_CAPTURE_OUT_IOS=docs/qa/2026-06-11-pristine-sweep/ios/store-screens-main-final corepack pnpm exec detox test -c ios.sim.debug e2e/store-screenshots.e2e.js --reuse --loglevel info --artifacts-location docs/qa/2026-06-11-pristine-sweep/detox-ios-store-screens-main-final`: passed. Evidence: `ios/store-screens-main-final/*.png` and `ios/store-screens-main-final/contact-sheet.png`. |
| Android store-surface capture, `qa-rich-history` | Pass | `env EXPO_DEV_SERVER_PORT=8081 EXPO_PUBLIC_DEV_LAUNCH_PRESET=qa-rich-history FLORIVA_CAPTURE=1 FLORIVA_CAPTURE_OUT_ANDROID=docs/qa/2026-06-11-pristine-sweep/android/store-screens-main-final corepack pnpm exec detox test -c android.emu.debug e2e/store-screenshots.e2e.js --reuse --loglevel info --artifacts-location docs/qa/2026-06-11-pristine-sweep/detox-android-store-screens-main-final`: passed with Detox's post-run open-handle notice after success. Evidence: `android/store-screens-main-final/*.png` and `android/store-screens-main-final/contact-sheet.png`. |
| Android reviewer-polish filtered capture, `qa-rich-history` | Pass | `env EXPO_DEV_SERVER_PORT=8081 EXPO_DEV_SERVER_HOST=127.0.0.1 EXPO_PUBLIC_DEV_LAUNCH_PRESET=qa-rich-history FLORIVA_CAPTURE=1 FLORIVA_CAPTURE_SCREENS=logging,privacy-settings,ttc-birth-control FLORIVA_CAPTURE_OUT_ANDROID=docs/qa/2026-06-11-pristine-sweep/android/reviewer-polish-final corepack pnpm exec detox test -c android.emu.debug e2e/store-screenshots.e2e.js --reuse --loglevel info --artifacts-location docs/qa/2026-06-11-pristine-sweep/detox-android-reviewer-polish-final`: passed with Detox's post-run open-handle notice after success. Final evidence refreshed in `android/store-screens-main-final/logging.png`, `privacy-settings.png`, `ttc-birth-control.png`, and `contact-sheet.png`; raw filtered captures are in `android/reviewer-polish-final/`. |
| Android Birth control filtered capture, `qa-rich-history` | Pass | `android/birth-control-layout-final-clean/ttc-birth-control.png`; confirms compact timing row, clipped back-action pill, and clean status bar. |
| Android Calendar filtered capture, `qa-rich-history` | Pass | `android/calendar-legend-fix/calendar.png`; confirms the separated Calendar key header and compact legend chips. |
| iOS import-ready manual capture | Pass visually | `ios/import-ready-current/import.jpg`. |
| iOS filtered import capture through Detox | Pass | `import-capture-rerun.md`: post-restart main-checkout rerun targeted a known healthy iPhone 17 simulator, connected through the Expo dev client, and captured the seeded import review screen. Evidence: `ios/import-ready-main-rerun/import.png`. Earlier blocked attempts remain documented in the rerun note. |
| iOS logged-state functional batch, `qa-rich-history` | Pass | `detox-ios-qa-rich-functional`: 6 suites, 6 tests. Covered private timeline, prediction preparedness, birth-control hub, TTC mode, condition modes, and pattern briefings. |
| Android logged-state functional batch, `qa-rich-history` | Partial, then fixed | Initial 6-suite batch passed 5 suites and failed Android condition scrolling. `detox-android-condition-fix` rerun passed the fixed condition suite. Combined evidence covers the same six suites. |
| iOS fresh install smoke and cold persistence, no preset | Pass after E2E helper fix | `detox-ios-fresh-functional-rerun2`: smoke and persistence passed, including real cold relaunch with SQLite data preserved. Earlier `detox-ios-fresh-functional` and `detox-ios-fresh-functional-rerun` record the fixed helper failures. |
| iOS delete all local data, no preset | Pass | `env DETOX_IOS_DEVICE_ID=2B9F547F-E6A8-409B-85EE-968CBA23DE20 EXPO_DEV_SERVER_PORT=8087 EXPO_PUBLIC_BILLING_E2E_MODE=local-purchase-success corepack pnpm exec detox test -c ios.sim.debug e2e/delete-all-data.e2e.js --reuse --loglevel info --artifacts-location docs/qa/2026-06-11-pristine-sweep/detox-ios-delete-all-data-db-proof-rerun`: 2 tests passed after the Android DB-proof harness changes. Covered real onboarding, real SQLite note write, Settings destructive delete, immediate onboarding return, and cold relaunch with the same app container. Screenshot evidence: `ios/delete-all-data-pass/cold-relaunch-onboarding.png`. |
| iOS fresh install smoke and cold persistence review-fix rerun | Pass | `detox-ios-fresh-review-fix-rerun2`: verifies the reviewed option-plus-Continue retry helper still passes smoke and real cold relaunch persistence. Earlier `detox-ios-fresh-review-fix*` directories record rejected `Selected`/attribute synchronization attempts. |
| Android fresh install smoke and mandatory paywall, no preset | Pass | `detox-android-fresh-functional`: smoke and fresh mandatory paywall passed under `EXPO_PUBLIC_BILLING_E2E_MODE=local-purchase-success`. |
| Android delete all local data, no preset | Pass | `env EXPO_DEV_SERVER_PORT=8087 EXPO_PUBLIC_BILLING_E2E_MODE=local-purchase-success corepack pnpm exec detox test -c android.emu.debug e2e/delete-all-data.e2e.js --reuse --loglevel info --artifacts-location docs/qa/2026-06-11-pristine-sweep/detox-android-delete-all-data-db-proof-final`: 2 tests passed. Covered real onboarding, real SQLite log write, direct pre-delete `daily_logs` row-count proof, Settings destructive delete, immediate onboarding return, direct post-delete row-count proof, cold relaunch with the same app container, and a direct post-run SQLite count of `0` for `2026-06-10`. Screenshot evidence: `android/delete-all-data-db-proof/cold-relaunch-onboarding.png`. |
| iOS mandatory onboarding paywall, no preset | Pass | `detox-ios-fresh-functional`: mandatory paywall passed and captured `ios-onboarding-paywall-mandatory.png`. |
| iOS grandfathered-expired full-lock paywall | Pass | `detox-ios-grandfathered-expired`: locked `/subscribe` flow passed and captured `ios-subscribe-lock-expired.png`. |
| Android grandfathered-expired full-lock paywall | Pass | `detox-android-grandfathered-expired`: locked `/subscribe` flow passed and captured `android-subscribe-lock-expired.png`. |
| iOS import concierge, `import-ready` | Pass | `detox-ios-import-ready`: seeded preview review, delete, and commit passed. |
| Android import concierge, `import-ready` | Pass | `detox-android-import-ready`: seeded preview review, delete, and commit passed. |
| Android DocumentsUI picker, `seeded-tracker` | Pass | `env EXPO_DEV_SERVER_PORT=8081 EXPO_PUBLIC_DEV_LAUNCH_PRESET=seeded-tracker EXPO_PUBLIC_BILLING_E2E_MODE=local-purchase-success corepack pnpm exec detox test -c android.emu.debug e2e/android-import-picker.e2e.js --reuse --loglevel info --artifacts-location docs/qa/2026-06-11-pristine-sweep/detox-android-import-picker-expanded-main-rerun7`: 5 tests passed. Covered native picker cancel, real Clue `.cluedata` selection, real Flo `.json` selection, malformed JSON rejection, and unsupported media rejection. Screenshots: `detox-android-import-picker-expanded-main-rerun7/.../android-import-picker-*.png`. |
| Android Today/logging/delete visual recapture, `seeded-tracker` | Pass visually | `android/post-restart-visual-rerun/today.png`, `logging.png`, and `delete-data-confirmation.png`. Confirms the floating dock does not cover Today content, the logging screen fits its first-viewport chips, and the final delete confirmation uses the solid danger action. |
| iOS Insights visual polish rerun, `qa-rich-history` | Pass visually | `env DETOX_IOS_DEVICE_ID=2B9F547F-E6A8-409B-85EE-968CBA23DE20 EXPO_DEV_SERVER_PORT=8081 EXPO_PUBLIC_DEV_LAUNCH_PRESET=qa-rich-history FLORIVA_CAPTURE=1 FLORIVA_CAPTURE_SCREENS=insights FLORIVA_CAPTURE_OUT_IOS=docs/qa/2026-06-11-pristine-sweep/ios/insights-main-rerun4 corepack pnpm exec detox test -c ios.sim.debug e2e/store-screenshots.e2e.js --reuse --loglevel info --artifacts-location docs/qa/2026-06-11-pristine-sweep/detox-ios-insights-main-rerun4`: passed. Screenshot: `ios/insights-main-rerun4/insights.png`. Confirms the singular title reads "What your last cycle says." and the tab scrim prevents text from being sliced by the dock. |
| Android Insights visual polish rerun, `qa-rich-history` | Pass visually | `env EXPO_DEV_SERVER_PORT=8081 EXPO_PUBLIC_DEV_LAUNCH_PRESET=qa-rich-history FLORIVA_CAPTURE=1 FLORIVA_CAPTURE_SCREENS=insights FLORIVA_CAPTURE_OUT_ANDROID=docs/qa/2026-06-11-pristine-sweep/android/insights-main-rerun2 corepack pnpm exec detox test -c android.emu.debug e2e/store-screenshots.e2e.js --reuse --loglevel info --artifacts-location docs/qa/2026-06-11-pristine-sweep/detox-android-insights-main-rerun2`: passed with Detox's post-run open-handle notice after success. Screenshot: `android/insights-main-rerun2/insights.png`. Confirms the dock no longer slices text and the fertility help icon renders as a circle, not a square-backed target. |
| iOS backup export, no preset | Pass | `detox-ios-backup-export`: encrypted backup creation and native handoff state passed, captured `backup-export-after-create.png`. |
| Android backup export, no preset | Pass | `detox-android-backup-export`: encrypted backup creation and native handoff state passed, captured `backup-export-after-create.png`. |
| iOS backup restore preview, `backup-ready` | Pass after E2E gating fix | `detox-ios-backup-ready-rerun`: restore preview passed and captured `backup-restore-preview-productized.png`. Earlier `detox-ios-backup-ready` records the now-fixed false export assertion. |
| Android backup restore preview, `backup-ready` | Pass | `detox-android-backup-ready`: restore preview passed and captured `backup-restore-preview-productized.png`. |
| iOS backup restore commit, `backup-ready` | Pass | `env DETOX_IOS_DEVICE_ID=2B9F547F-E6A8-409B-85EE-968CBA23DE20 EXPO_DEV_SERVER_PORT=8087 EXPO_PUBLIC_DEV_LAUNCH_PRESET=backup-ready EXPO_PUBLIC_BILLING_E2E_MODE=local-purchase-success corepack pnpm exec detox test -c ios.sim.debug e2e/backup-export.e2e.js --reuse --loglevel info --artifacts-location docs/qa/2026-06-11-pristine-sweep/detox-ios-backup-restore-commit-final`: restore commit passed while export was skipped by preset. Covered seeded preview, pre-commit SQLite state (`2026-04-13` absent and prior `2026-04-03` present), replacement acknowledgement, destructive confirm, success status, and post-commit SQLite state (`2026-04-13` present and `2026-04-03` gone). SQLite snapshot helper copies WAL/SHM sidecars when present. Evidence: `detox-ios-backup-restore-commit-final/.../backup-restore-committed.png`. |
| Release preflight, local ignored artifacts | Pass | Loaded `.env.local` and Android signing values from `credentials.json` without printing secrets, then ran `scripts/check-release-env.js` with `APP_ENV=production`: passed. The default ignored export path `build/release/AppStoreExport/` was refreshed from `build/release/AppStoreExport-1.1.2-14/`; the IPA metadata is bundle `app.floriva`, version `1.1.2`, build `14`, executable `Floriva`, Apple Distribution signed with team `TEAMID1234`, and `get-task-allow=false`. |
| Android backup restore commit, `backup-ready` | Pass | `env EXPO_DEV_SERVER_PORT=8087 EXPO_PUBLIC_DEV_LAUNCH_PRESET=backup-ready EXPO_PUBLIC_BILLING_E2E_MODE=local-purchase-success corepack pnpm exec detox test -c android.emu.debug e2e/backup-export.e2e.js --reuse --loglevel info --artifacts-location docs/qa/2026-06-11-pristine-sweep/detox-android-backup-restore-commit-final`: restore commit passed while export was skipped by preset. Covered seeded preview, pre-commit SQLite state (`2026-04-13` absent and prior `2026-04-03` present), replacement acknowledgement, destructive confirm, success status, and post-commit SQLite state (`2026-04-13` present and `2026-04-03` gone). SQLite snapshot helper copies WAL/SHM sidecars when present. Evidence: `detox-android-backup-restore-commit-final/.../backup-restore-committed.png`. |
| iOS biometric lock, `locked-app` | Pass | `detox-ios-locked-app`: cold locked state, fail-closed biometric attempt, and successful unlock passed. Existing relock-after-background test remains skipped by the suite. |
| iOS reminder scheduling, `seeded-tracker` | Pass | `reminder-diagnostics-runs.md`: seeded active row, enable new reminder, cancel reminder, exact OS scheduled-notification set, and generic OS notification copy passed. Earlier failed reruns exposed the fixed Detox matcher and iOS trigger-shape assumptions. |
| Android reminder scheduling, `seeded-tracker` | Pass | `reminder-diagnostics-runs.md`: seeded active row, enable new reminder, cancel reminder, exact OS scheduled-notification set, and generic OS notification copy passed. Earlier failed rerun exposed the fixed Android trigger-shape assumption. |
| iOS condition-mode review-fix rerun, `qa-rich-history` | Pass | `detox-ios-condition-review-fix`: verifies bounded condition-row scroll helper. |
| Android condition-mode review-fix rerun, `qa-rich-history` | Pass | `detox-android-condition-review-fix`: verifies bounded condition-row scroll helper on the emulator. |
| iOS StoreKit scheme build-run | Pass | XcodeBuildMCP `FlorivaStoreKit` build-run succeeded after retry. Build log: `~/Library/Developer/XcodeBuildMCP/workspaces/floriva-c959e432fa22/logs/build_run_sim_2026-06-11T17-25-18-856Z_pid79717_303ea2b9.log`. |
| iOS native purchase handoff, `FlorivaStoreKit` | Partial | Onboarding-to-paywall path captured in `ios/storekit-simulator/*.jpg`. Tapping `Choose annual plan` opens the native Apple Account sign-in sheet, proving the flow reaches native StoreKit rather than the fake E2E purchase path. Completed entitlement is not verified in this pass. |
| iOS sandbox credential entry on simulator | Blocked | Sandbox keys were sourced from the main checkout `.env.local` without printing values. Simulator StoreKit sign-in did not accept keystroke or pasteboard entry and returned `You must enter both your Apple Account and password.` `xcrun storekit` is unavailable in this Xcode install, so the local `.storekit` file could not be attached through the CLI. |
| iOS StoreKit native rerun | Partial, account-completion blocked | `storekit-native-rerun.md`: after `pod install` fixed the local CocoaPods sandbox mismatch, XcodeBuildMCP `FlorivaStoreKit` build-run succeeded on iPhone 17. Clean Metro served without fake billing mode, the paywall rendered, and tapping `Choose annual plan` opened the native Apple Account sheet. Sandbox credential entry dismissed the sheet but did not unlock or present purchase confirmation. Evidence: `ios/storekit-rerun-main/paywall-annual-cta.jpg` and `ios/storekit-rerun-main/apple-account-sign-in.jpg`. |

## Review Pass

- Dedicated sub-agent review found three issues: onboarding helper no longer proving selection before Continue, condition-mode relying on one geometry-dependent swipe, and CycleRibbon wrapping not covered under constrained width.
- All three were addressed and rerun: fresh iOS smoke/persistence, iOS/Android condition-mode, focused CycleRibbon unit test, lint, typecheck, and full coverage.
- Android delete-all-data sub-agent review found the first Android helper used fixed viewport assumptions and overstated the source-level sanity test. The helper now derives Android viewport size from `wm size`, closes the Expo dev menu through visible UI nodes, the README labels the sanity test as a source-level guard, and the native Android delete-all-data Detox run passed again after those fixes.
- Focused delete-all-data review then found Android still proved route reset more strongly than data deletion. The spec now pulls `files/SQLite/floriva.db` via `run-as`, fails closed if the DB cannot be inspected, verifies the Android probe row exists before deletion, verifies zero matching rows immediately after deletion and after cold relaunch, and the README no longer overstates route-only evidence.
- Continuation sub-agent code review found no blocking issue in the recovered diff, but called out the Android picker and backup E2E specs as environment-sensitive and worth rerunning on target devices.
- Continuation E2E-gap review confirmed StoreKit purchase completion and iOS filtered import capture are environment-blocked rather than app-logic-blocked, and recommended expanding Android picker coverage for Flo and unsupported media. Android native picker proof now passes for cancel, Clue, Flo, malformed JSON, and unsupported media after fixing the dev-client setup and media MIME reachability.

## Visual Review Notes

- Current final iOS and Android contact sheets are in `ios/store-screens-main-final/contact-sheet.png` and `android/store-screens-main-final/contact-sheet.png`. Older contact sheets remain in `ios/store-screens-current-contact-sheet.jpg` and `android/store-screens-current-contact-sheet.jpg`.
- Independent visual sub-agent review found no broken rendering, missing assets, or text overlap, but flagged four polish items: muted-looking onboarding Continue buttons, Today content sitting too close to the floating dock, literary first-run "almanac" copy, and a crowded Calendar legend/info icon row.
- Addressed all four visual findings from that review in code: active onboarding Continue buttons now have a stronger raised state, shared tab clearance was increased, the welcome headline now says "tracker," and the Calendar key now separates help from the legend chips. The Calendar fix was recaptured in `android/calendar-legend-fix/calendar.png`.
- Continuation visual review re-flagged the old StoreKit welcome screenshot because `ios/storekit-simulator/01-onboarding-welcome.jpg` still shows "almanac"; the refreshed main-checkout evidence in `ios/storekit-rerun-main/welcome-tracker-copy.png` now shows the current "A private tracker for your cycle." copy with a clean 9:41 status bar.
- Continuation visual review also found the final delete-data confirmation affordance too neutral in older screenshots. The destructive button token now uses a solid danger fill; Android recapture in `android/post-restart-visual-rerun/delete-data-confirmation.png` verifies the final confirmation now reads as destructive while the cancel action remains secondary.
- Birth-control and older insights screenshots showed lower content partially entering the viewport. The Birth control screen also showed stacked reminder timing buttons that made the first viewport feel too tall and slow to scan; the controls were tightened in code and verified in `android/birth-control-layout-final-clean/ttc-birth-control.png`. The filtered Android recapture also caught and fixed the rectangular back-action fill and noisy Android demo network indicators. The Insights reruns in `ios/insights-main-rerun4/insights.png` and `android/insights-main-rerun2/insights.png` verify the singular title grammar, shared tab scrim, and circular help affordance fix.
- Android Today/logging post-restart captures in `android/post-restart-visual-rerun/` show the dock no longer covers Today content and the logging first viewport fits without overlap.
- Reviewer-polish Android recapture removed the stale mid-scroll logging frame from the final contact sheet, tightened the privacy uninstall sentence, and refreshed the Birth control shot with clean demo status-bar chrome. Evidence: `android/reviewer-polish-final/` and `android/store-screens-main-final/contact-sheet.png`.
- The iOS paywall primary action can be enabled under a true Xcode `FlorivaStoreKit` Run action with StoreKit testing attached, but the headless MCP/simctl rerun cannot attach the StoreKit configuration and leaves the CTA disabled because no products load. The 2026-06-12 rerun proves local build/install/launch is healthy after CocoaPods sync, validates product ID/config alignment, and confirms transaction completion still needs Xcode GUI StoreKit controls or a real-device/TestFlight sandbox pass.

## Remaining Sweep Work

- Complete Apple purchase validation past native sign-in: local build/install/launch is healthy, but simulator Apple Account entry did not complete the transaction. Use Xcode GUI StoreKit controls with `FlorivaStoreKit` and `ios/Floriva.storekit` attached, or use a real-device/TestFlight sandbox pass with the `.env.local` sandbox tester. Do not print credentials.
- Continue visual/manual walkthroughs beyond the covered E2E paths until the app has no remaining functional, UI, or UX issues worth fixing.
